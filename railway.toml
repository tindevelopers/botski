[build]
builder = "dockerfile"
dockerfilePath = "recall/Dockerfile"
# Use repo root as source (do not set Root Directory in Railway, or leave blank)

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"

[env]
NODE_ENV = "production"
PORT = "3003"

